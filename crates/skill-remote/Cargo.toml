[package]
name = "skill-remote"
version = "0.1.0"
edition = "2021"
description = "SSH remote sessions: provision + launch skill-server on a remote box and tunnel to it (the VS Code-remote model)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "5"
